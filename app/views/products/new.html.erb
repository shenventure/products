<div class = "container">

<h1>Add a New Product</h1>

<% if defined? flash %>
	<% if flash[:success] %>
		<p><%= flash[:success] %></p>
	<% elsif flash[:errors] %>
		<% flash[:errors].each do |error| %>
		    <p><%= error %></p>
		   <% end %>
	<% end %>
<% end %>

	<%= simple_form_for @Product do |p| %>  
	<%= p.input :name %>
	<%= p.association :category, as: :select, include_blank: false %>	
	<%= p.input :description %>
	<%= p.input :pricing %>
	<%= p.button  :submit, class: "btn btn-primary" %>
	<% end %>
</div>